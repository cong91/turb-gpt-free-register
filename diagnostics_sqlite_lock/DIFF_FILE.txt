diff --git a/diagnostics_sqlite_lock/ORIGINAL_FILE.py b/diagnostics_sqlite_lock/MODIFIED_FILE.py
index 9560c4d..7b117f9 100644
--- a/diagnostics_sqlite_lock/ORIGINAL_FILE.py
+++ b/diagnostics_sqlite_lock/MODIFIED_FILE.py
@@ -34,7 +34,7 @@ def is_app_state_path(path: str | Path) -> bool:
     return Path(path).resolve() == APP_STATE_DB_PATH.resolve()
 
 
-def connect(path: str | Path | None = None, *, busy_timeout_ms: int = 5000) -> sqlite3.Connection:
+def connect(path: str | Path | None = None, *, busy_timeout_ms: int = 30000) -> sqlite3.Connection:
     """Open a configured SQLite connection for application state."""
     target = Path(path) if path is not None else APP_STATE_DB_PATH
     target.parent.mkdir(parents=True, exist_ok=True)
@@ -46,7 +46,11 @@ def connect(path: str | Path | None = None, *, busy_timeout_ms: int = 5000) -> s
     )
     connection.row_factory = sqlite3.Row
     connection.execute(f"PRAGMA busy_timeout = {max(1, int(busy_timeout_ms))}")
-    connection.execute("PRAGMA journal_mode = DELETE")
+    # Reassert rollback journaling only when the database is not already in
+    # the required mode; changing journal mode takes an exclusive lock.
+    journal_mode = str(connection.execute("PRAGMA journal_mode").fetchone()[0]).lower()
+    if journal_mode != "delete":
+        connection.execute("PRAGMA journal_mode = DELETE")
     connection.execute("PRAGMA foreign_keys = ON")
     connection.execute("PRAGMA synchronous = FULL")
     return connection

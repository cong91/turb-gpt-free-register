# ============================================================
# 运行时账号凭证 / 真实数据 —— 永远不要提交
# ============================================================

# Outlook / API 邮箱池（含密码、clientId、refreshToken 或私有取码地址）
用于注册的邮箱.json
用于注册的邮箱.txt
用于注册的API邮箱.json
用于注册的API邮箱.txt
用于注册的Gmail API邮箱.json
用于注册的Gmail API邮箱.txt

# 注册成功账号（含 accessToken、TOTP secret 等）
注册成功的邮箱.json
注册成功的邮箱.txt
注册成功的token.txt
accounts/
H_API.md

# Codex 授权凭证（含 access/refresh/id token）
codex_accounts/
codex_导出状态.json
codex_agent_accounts

# 任务记录与运行日志
注册任务.json
注册日志/
accounts_viewer.html

# Cloudflare 域名邮箱池（运行时数据）
用于注册的域名邮箱.json
gmail_cdk_ledger.json
paymesh_card_ledger.json

# 旧版数据/历史迁移残留
data/

# CDK inventory SQLite runtime data
cdk_inventory.sqlite3
cdk_inventory.sqlite3-journal
cdk_inventory.sqlite3-wal
cdk_inventory.sqlite3-shm
cdk_inventory.sqlite3.backup
cdk_inventory.sqlite3.staging
cdk_inventory.sqlite3.backup.*
cdk_inventory-*.sqlite3.backup
otp_identity.sqlite3
otp_identity.sqlite3-journal
otp_identity.sqlite3-wal
otp_identity.sqlite3-shm
gmail_cdk_batches.sqlite3
gmail_cdk_batches.sqlite3-journal
gmail_cdk_batches.sqlite3-wal
gmail_cdk_batches.sqlite3-shm

# Roxy profile manager runtime catalog and encrypted archives
roxy_profile_manager*.sqlite3
roxy_profile_manager*.sqlite3-*
roxy_profile_archives/
roxy_profile_staging/

# ============================================================
# Canonical application database (origin + fork state runtime source of truth)
turb.sqlite3
turb.sqlite3-journal
turb.sqlite3-wal
turb.sqlite3-shm
turb.sqlite3.*

# Fork migration input and rollback/legacy runtime databases
用于注册的Gmail API邮箱_batches.db
registration_jobs.db

# ============================================================
# 缓存 / 临时文件 / 诊断输出
# ============================================================
__pycache__/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
.venv/
*.py[cod]
*$py.class
*.so
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 历史调试输出（_xxx.out / _xxx_dump.html）
tools/_*.out
tools/_*.html
tools/_*.py
!tools/test_codex_oauth.py

# Local browser/protocol captures and derived reports
are-capture-*.jsonl
Default-all-domains-*.json
docs/protocol_har_summary.json
*.har

# Sentinel runner 临时 challenge 文件
sentinel-challenge-*.json

# Sentinel 本地调试配置（可能含 Cookie/Bearer/Challenge）
sentinel.config.json
tools/sentinel.config.json
sentinel/sentinel.config.json

# 意外生成的本地目录
-/

# ============================================================
# 编辑器 / 操作系统
# ============================================================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ============================================================
# 本地实验脚本（任何 .local. 前缀的私有文件）
# ============================================================
*.local.*

# Local secrets
.env
.env.*
!.env.example
secrets/
webui.log
webui.pid
logs/
run/

# Local agent/workflow state
.codex/
.zcode/
.beads/
.planning/
/nul

# Registration task snapshots may contain real account identities
注册任务.json.backup-*
